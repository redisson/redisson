---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "Redisson Cache"
metadata:
  keywords:
    - "redisson"
    - "cache"
  categories:
    - "data"
    - "reactive"
  status: "stable"
  config:
    - "quarkus.cache.redisson"